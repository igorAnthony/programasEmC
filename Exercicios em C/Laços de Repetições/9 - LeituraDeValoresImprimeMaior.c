#include <stdio.h>
#include <stdlib.h>
/*Elabore um programa que faca leitura de varios numeros inteiros, ate que se digite
um numero negativo. O programa tem que retornar o maior e o menor numero lido.*/
//Função Principal
int main()
{
    //Definindo variaveis
    int valor = 0, tam = 0, i;
    int v[];
    //Laço de repetição para pegar os valores
    for(i = 0; i >= 0; i++){
        scanf("%d", v[i]);
        tam++;
    }for(i = 0; i <= tam; i++)


}
