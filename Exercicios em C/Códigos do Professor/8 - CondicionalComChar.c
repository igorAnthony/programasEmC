#include <stdlib.h>
#include <stdio.h>

int main()
{
    //Definindo variaveis
    char letra = 'x';

    //Condicional simples
    if(letra == 'x')
    {
        printf("A letra eh x.\n");
    }
        printf("A letra na tabela ASCII eh %d.\n", letra);
    if(letra == 120)
    {
        printf("A letra eh x.\n");
    }
    //Pausa o programa após executar
    system("pause");
}

